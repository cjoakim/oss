
export class Speed {
    
    constructor(
        public verbose: boolean = false) {
        // no statements required
    }

    greet(name: string): string {
        return `hello ${name}!`;
    }

}
